name: wdpr-payment-controls-api
stack: node
baseBranch: develop

commands:
  build: npm run build
  test: npm test
  lint: npm run lint
  format: npm run format

integrations:
  jira:
    projectKey: DPAY
    statuses:
      inProgress: "In Progress"
      review: "Code Review"
      done: "Done"
  github:
    org: SANCR225
    repo: wdpr-payment-controls-api

workspace:
  specsDir: docs/specs/
  useSpecs: false
