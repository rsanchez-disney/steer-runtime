# Technology Standards

- .NET 9 by default unless the workspace states otherwise
- xUnit by default unless the workspace states otherwise
- Moq by default unless the workspace states otherwise
- strongly typed options for configuration
- explicit dependency registration
- AWS SDK usage only behind abstractions or adapters
