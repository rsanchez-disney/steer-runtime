# .NET Company Common Library Integration

Use the configured company common library path as the source of truth for reusable cross-cutting capabilities.

Inspect:
- registration extensions
- reusable abstractions
- reusable options
- middleware or pipeline helpers
- diagnostics and logging features
- storage or AWS-related adapters if present

Prefer reuse over duplication.
