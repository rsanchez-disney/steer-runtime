# .NET Self-Host API Testing Standards

Minimum coverage:
- success path
- validation failure
- dependency failure
- not found or empty result
- mapping behavior where relevant

Keep tests deterministic and independent from live infrastructure.
