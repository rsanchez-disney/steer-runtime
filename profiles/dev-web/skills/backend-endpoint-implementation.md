# Skill: Backend endpoint implementation (Java)

Checklist:
- Ensure request/response models are backward compatible
- Validate inputs centrally (validators)
- Use service layer; controllers should be thin
- Add unit tests for new behavior and error branches
