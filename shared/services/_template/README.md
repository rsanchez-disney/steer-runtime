# {Service Name}

This is a service bank template. Copy this directory to create a new service bank:

```bash
cp -r shared/services/_template shared/services/my-service
```

Then fill in each file with the service's actual details.

## Files
| File | Purpose |
|---|---|
| `api-contracts.md` | API overview, base URLs, key endpoints, auth |
| `architecture.md` | Tech stack, dependencies, data flow, deployment |
| `endpoints.md` | Detailed endpoint documentation |
| `patterns.md` | Error handling, pagination, idempotency, gotchas |
