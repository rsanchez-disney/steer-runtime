# Team-shared defaults (non-secret values only)
# These are committed to the repo and shared across the team.
# User-specific or secret values go in ~/.kiro/tokens.env (never committed).
#
# Resolution order: tokens.env > defaults.env > mcp.json variable.default

EXAMPLE_API_URL=https://latest.example.wdprapps.disney.com
