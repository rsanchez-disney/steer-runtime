{
  "name": "inspector-inspect-app",
  "description": "Inspector orchestrator should delegate UI inspection to ui_inspector_agent or web_discovery_agent",
  "agent": "inspector_orchestrator",
  "prompt": "Inspect the running app at http://localhost:3000 and identify accessibility issues on the checkout page",
  "expect": {
    "delegated": true,
    "agents_called_any": ["ui_inspector_agent", "web_discovery_agent", "ux_specialist_agent", "web_scraping_validator_agent"],
    "forbidden_tools": ["chrome_navigate", "chrome_evaluate"]
  },
  "timeout_seconds": 180
}
