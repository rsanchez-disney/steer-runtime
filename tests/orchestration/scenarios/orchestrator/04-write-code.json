{
  "name": "write-code",
  "description": "Orchestrator should delegate code writing to backend/webapi specialist",
  "agent": "orchestrator",
  "prompt": "Add a new utility method validateRefundAmount(amount, originalCharge) to the RefundValidator class in wdpr-payment-controls-api. It should return true if amount <= originalCharge.",
  "expect": {
    "delegated": true,
    "agents_called_any": ["backend", "webapi", "devops_runner_agent"],
    "forbidden_tools": ["fs_write", "code", "execute_bash"]
  },
  "timeout_seconds": 180
}
