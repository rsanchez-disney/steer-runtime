# Project mappings

No projects configured in the default workspace.

Activate a team workspace to get project mappings with:

- Repository locations and paths
- Jira prefix to project mapping
- Tech stack identification per repo
- Cross-repo dependency maps

Run `koda workspace list` to see available options.
