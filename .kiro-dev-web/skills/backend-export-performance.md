# Skill: Export performance (Backend)

Checklist:
- Identify bottlenecks with timing logs (DEBUG)
- Batch client processing and DB calls
- Avoid loading all clients into memory when possible
- Use streaming output or page results to WebAPI if architecture allows
- Verify behavior under large client counts
